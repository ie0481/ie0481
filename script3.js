const toggle = document.querySelector('.toggle')

const showcase = document.querySelector('.showcase')

toggle.addEventListener('click', () ==>{
    toggle.classlist.toggle('active')})
showcase.className.toggle('active')